---
title: Gate Hardening — Content Quality + Adversarial Verification Plan
scope: Standard
status: approved
created: 2026-04-16
source: docs/brainstorms/2026-04-16-hrc038-requirements.md
tasks: 3
complexity: medium
---

## Problem Frame

Gates check form not substance. No independent adversarial verification. Fix both as real-time blockers in `runStructuralGate`.

## Decision Log

| Decision | Rationale | Rejected |
|----------|-----------|----------|
| Tier 1 exempt via brainstorm frontmatter `scope: Lightweight` | Already present in artifacts; no new state needed | Check `.apex/state.json` tier — not tracked there |
| Adversarial verification file path: `.apex/verifications/{stage}-adversarial.md` | Under `.apex/` keeps it out of source tree | `docs/verifications/` — pollutes docs |
| Gate prints sub-agent dispatch prompt on adversarial block | Agent knows exactly what to do; no guessing | Link to docs — adds indirection |

## File Manifest

| Action | Path |
|--------|------|
| Modify | `src/state/state.ts` (add CQ + adversarial checks to brainstorm & review gates) |
| Modify | `src/__tests__/stage-gates.test.ts` (add tests for new checks) |

### Test Files

| Path | Tests |
|------|-------|
| `src/__tests__/stage-gates.test.ts` | CQ1 brainstorm AC count, CQ3 review persona content, adversarial file gate |

## Task Decomposition

| ID | Description | Files | Complexity | Deps | AC |
|----|-------------|-------|------------|------|-----|
| T1 | Add CQ1 (AC count >= 3) + adversarial file check to brainstorm gate | `src/state/state.ts` | small | — | AC1, AC3, AC5 |
| T2 | Add CQ3 (persona content > 50 chars) + adversarial file check to review gate | `src/state/state.ts` | small | T1 | AC2, AC4, AC5 |
| T3 | Tests + build + verification | `src/__tests__/stage-gates.test.ts` | medium | T1, T2 | AC6, AC7 |

## Test Plan

| AC | Scenario | Test File |
|----|----------|-----------|
| AC1 | Given brainstorm artifact with 2 AC items, when gate runs, then CQ1 fails | `stage-gates.test.ts` |
| AC2 | Given review artifact with "No findings" (< 50 chars) in Security section, when gate runs, then CQ3 fails | `stage-gates.test.ts` |
| AC3 | Given no `.apex/verifications/brainstorm-adversarial.md`, when gate runs on non-Lightweight scope, then blocked | `stage-gates.test.ts` |
| AC5 | Given scope=Lightweight, when gate runs, then adversarial check skipped | `stage-gates.test.ts` |
