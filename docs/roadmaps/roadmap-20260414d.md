---
title: Apex Forge Roadmap
snapshot_date: 2026-04-14d
trigger: session compound — compound memory write enforcement
based_on:
  - roadmap-20260414c.md
---

# Apex Forge Roadmap — 2026-04-14d

## Just Completed

- **Compound memory write enforcement** — HARD GATE + exit gate S5 check. Commit `652c6e6`

## Completed (from previous snapshots)

- Pipeline re-entry chain, CI type check, Dashboard telemetry, Skill auto-trace hook
- Push > Pull enforcement, Instrumentation via hooks
- 6-stage pipeline, Exit Gates, Background skill update
- Multi-agent orchestrator, Master Agent, Dashboard Hub/PWA

## Open — High Priority

### 1. Design-baseline Phase 2 实现
- 多 Persona sub-agent 设计评审

### 2. 其他阶段内容质量门扩展
- Plan/Review 阶段的 Evidentiary Discipline

### 3. Ship Step 6a 合规强制
- Agent 跳过 Iteration Summary 的合规率为 0%。需要 hook 或 gate 强制执行

## Open — Medium Priority

### 4. Dashboard Hub 默认项目问题
- Hub 端口 3456 默认显示最后活跃项目，不是当前项目

### 5. Linux open-file 兼容
- `/api/open-file` 用 `open -R` (macOS only)，需要 `xdg-open` fallback

### 6. Skill trace output_status 精度
- Hook 固定写 "completed"，无法获取 skill 语义状态

### 7. WorkerResult.findings 类型对齐
- 声明 `string[]` 但运行时处理 `FindingLike` 对象

## Open — Low Priority

### 8. Compound 自动化
### 9. 跨项目验证

## Parked (Not Active)

- pipeline 可视化
- 多 Agent 场景下的 stage 锁
- Design-baseline Phase 3
