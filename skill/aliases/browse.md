---
name: apex-forge-browse
description: "Alias: routes to /browser-qa-testing"
---

**This command is now provided by an independent skill.**

Load and follow `/browser-qa-testing` — browser automation is now integrated into the QA testing skill.

For browser commands, use the `browse` binary directly (e.g., `browse goto <url>`).
